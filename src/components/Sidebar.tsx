// import { useAuth } from '@/contexts/AuthContext';

const Sidebar = () => {
  // const { logout } = useAuth();
  return (
    <>
      {/* Sidebar content here */}
      <div className="h-full grid place-items-center">
        <a className="text-xl">Options coming soon!</a>
      </div>
    </>
  );
};

export default Sidebar;
