# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for required secrets
        run: |
          if [ -z "${{ secrets.VITE_FIREBASE_API_KEY }}" ]; then echo "Missing VITE_FIREBASE_API_KEY"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" ]; then echo "Missing VITE_FIREBASE_AUTH_DOMAIN"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_PROJECT_ID }}" ]; then echo "Missing VITE_FIREBASE_PROJECT_ID"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" ]; then echo "Missing VITE_FIREBASE_STORAGE_BUCKET"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" ]; then echo "Missing VITE_FIREBASE_MESSAGING_SENDER_ID"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_APP_ID }}" ]; then echo "Missing VITE_FIREBASE_APP_ID"; exit 1; fi
          if [ -z "${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}" ]; then echo "Missing VITE_FIREBASE_MEASUREMENT_ID"; exit 1; fi
      - name: Build project
        run: yarn && yarn run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_WHO_WON_4E665 }}'
          projectId: who-won-4e665
